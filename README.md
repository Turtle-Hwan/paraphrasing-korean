# paraphrasing-korean — 한국어 문장 다듬기

한국어 글을 다듬는 도구입니다. **뜻은 한 글자도 바꾸지 않습니다.** 군더더기만 덜어 냅니다.

오직 한국어만을 위해 만들었습니다. 영어를 옮긴 듯한 말투를 걷어 내고 한국인이 읽기에 자연스러운
문장으로 되돌립니다. 글을 통째로 고치지 않습니다. 문장을 하나씩 따로 읽고 손봅니다.

손보는 것은 세 가지입니다.

- **번역투** — 영어를 직역한 듯한 어색한 말투
- **AI 티** — 챗봇이 쓴 듯한 기계적인 말투
- **문장 다이어트** — 없어도 되는 군더더기

문장 다이어트 규칙은 책 『내 문장이 그렇게 이상한가요?』에서 영감을 받았습니다.

```
다듬기 전:  데이터 분석을 통해 얻어진 결과는 매우 의미하는 바가 크다고 할 수 있다.
다듬은 뒤:  데이터를 분석해 얻은 결과는 매우 의미가 크다.
```

## 명령어

하나만 쓰고 싶으면 따로 부릅니다. 다 적용하려면 통합 명령을 씁니다.

| 명령어 | 하는 일 |
|---|---|
| `/paraphrasing-korean` | **통합**. 번역투 → AI 티 → 문장 다이어트를 한 문장씩 차례로 적용 |
| `/paraphrasing-korean-sentence` | 문장 다이어트. 적·의·것·들, 군더더기 "있다", 군더더기 피동, 조사 다듬기 |
| `/paraphrasing-korean-trans` | 번역투 빼기. 피동→능동, 가지다·통해·대해, 무생물 주어, 대명사 남용 |
| `/paraphrasing-korean-ai` | AI 티 빼기. 첫째·둘째 나열, 결말 공식, 과장 어휘, 이모지, 괄호·쉼표 남용 |

설치하면 명령어로 부릅니다. 그냥 "이 글 문장 다듬어줘" 처럼 말해도 알아서 골라 줍니다.

## 설치

여러 AI 도구에서 그대로 씁니다. Claude Code · Codex · OpenClaw · Hermes · Gemini CLI 를 지원합니다.

**Claude Code — 한 줄 설치**

```
/plugin marketplace add Turtle-Hwan/paraphrasing-korean
/plugin install paraphrasing-korean@paraphrasing-korean
```

**클론 + 스크립트 — 모든 도구**

```bash
git clone https://github.com/Turtle-Hwan/paraphrasing-korean
cd paraphrasing-korean
./install.sh            # 깔려 있는 도구를 스스로 찾아 설치
```

`./install.sh` 는 기본으로 심링크를 겁니다. `git pull` 만 하면 최신으로 갱신됩니다. 복사로 깔려면
`--copy` 를 붙입니다. 특정 도구만 깔려면 `--claude-only` 처럼 씁니다.

```bash
./update.sh             # 새 버전 확인하고 적용
./uninstall.sh          # 설치 제거
```

## 동작 방식

핵심은 **문장 단위로 다 넣고 빼서 다듬기**입니다. 정규식으로 한꺼번에 바꾸지 않습니다. 한 문장이라도
빠뜨리지 않으려고 작은 도구 두 개로 흐름을 묶었습니다.

1. **자르기** — `scripts/segment.py` 가 글을 문장 부호 기준으로 잘라 작업표를 만듭니다.
   제목·목록·코드 블록은 손대지 않습니다.
2. **다듬기** — 에이전트가 작업표의 문장을 위에서 아래로 하나씩 읽고 고칩니다. 고칠 게 없으면 그대로 둡니다.
3. **되붙이기** — `scripts/reassemble.py` 가 다듬은 문장을 원래 구조에 맞춰 다시 한 편으로 잇습니다.

되붙일 때 두 가지를 자동으로 지킵니다. 들어간 문장 수와 나온 문장 수가 다르면 멈춥니다. 너무 많이
바뀌어도 멈춥니다. 둘 다 원문을 망치지 않으려는 장치입니다.

원하면 `scripts/scan.py` 로 정규식 힌트를 켤 수 있습니다. 의심 가는 문장에 규칙 번호를 살짝 달아 줍니다.
다듬는 일은 어디까지나 에이전트가 합니다.

## 규칙

규칙은 모두 `references/` 폴더에 글로 정리했습니다.

- `sentence-rules.md` — 문장 다이어트. 책 『내 문장이 그렇게 이상한가요?』(저자 김정선, 유유, 2016)에서
  영감을 받았습니다.
- `translationese-rules.md` — 번역투. 영어 직역 흔적을 자연스러운 한국어로 바꿉니다.
- `ai-tell-rules.md` — AI 티. 챗봇 특유의 말투와 구조를 손봅니다.
- `prime-directives.md` — 모든 명령이 함께 지키는 약속. 괄호와 쉼표를 아끼는 한국어다운 문체도 여기 있습니다.

각 규칙은 "무엇을 찾고 어떻게 고치고 예를 들면 이렇게" 형식입니다. 예시만 읽어도 감이 옵니다.

## 기여

번역투와 AI 티 예시는 모을수록 좋아집니다. 새 예시와 새 규칙은 누구나 보탤 수 있습니다.
방법은 [CONTRIBUTING.md](CONTRIBUTING.md) 에 있습니다.

## 개발

```bash
python3 -m unittest discover -s tests
```

따로 깔 것 없이 파이썬 표준 기능만 씁니다.

## 라이선스

MIT. 다만 책 규칙의 저작권은 원저작자에게 있습니다. 이 저장소는 그 책에서 영감을 받아 도구로 옮긴
것입니다. 책 내용을 그대로 옮겨 싣지는 않았습니다.
